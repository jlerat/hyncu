[nosetests]
with-coverage=1
cover-package=hyncu
cover-erase=1
with-xunit=1
verbose=1

# See the docstring in versioneer.py for instructions. Note that you must
# re-run 'versioneer.py setup' after changing this section, and commit the
# resulting files.

[versioneer]
VCS = git
style = pep440
versionfile_source = hyncu/_version.py
versionfile_build = hyncu/_version.py
tag_prefix = v
parentdir_prefix = hyncu/_version.py

